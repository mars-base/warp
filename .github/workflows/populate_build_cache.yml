# Disabled: this workflow pushes to master branch and requires namespace runners
# not available on forks. Keep file but don't auto-trigger.
# on:
#   push:
#     branches:
#       - master
#     paths:
#       - 'Cargo.lock'
#       - '**/Cargo.toml'
#       - '.cargo/config.toml'
#       - 'rust-toolchain.toml'
#   schedule:
#     - cron: "0 8 * * 1-5"
#   workflow_dispatch:

on: workflow_dispatch

name: Populate Build Cache

jobs:
  populate_build_cache:
    name: CI
    uses: ./.github/workflows/ci.yml
    secrets: inherit
